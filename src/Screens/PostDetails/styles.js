import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    commntCon:{
        margin:20,
        height:60,
        alignItems:'center',
        paddingHorizontal:20, 
        backgroundColor:'orange',
        justifyContent:'space-evenly', borderRadius:10
    },
    textSty:{
        fontSize:16,
        color:'white', 
    }
})

export default styles